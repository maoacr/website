Drop final project imagery here (recommended: 1200×900 or larger, .webp)
and update the `image` field for the matching project in
`lib/data/projects.ts`. Until then, the site uses deterministic
picsum.photos placeholders so builds stay stable.
